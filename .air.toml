root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./bin/api ./cmd/api"
  entrypoint = "./bin/api"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["bin", "tmp"]
  delay = 1000
  stop_on_error = true

[misc]
  clean_on_exit = false
